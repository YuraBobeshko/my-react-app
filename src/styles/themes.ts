import {ThemeOptions} from '@mui/material';

export const lightTheme: ThemeOptions = {
    palette: {
        mode: 'light', // Use 'light' as the string literal
    },
};

export const darkTheme: ThemeOptions = {
    palette: {
        mode: 'dark', // Use 'dark' as the string literal
    },
};
